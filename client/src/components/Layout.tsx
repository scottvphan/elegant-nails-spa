import styled from "styled-components";
import { Header } from "./Header";

const LayoutContainer = styled.div``;

export const Layout = () => {
  return (
    <LayoutContainer>
      <Header />
    </LayoutContainer>
  );
};
